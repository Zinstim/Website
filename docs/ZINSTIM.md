# ZINSTIM — MASTER

Read this file first, every session, before anything else.

---

## 0. WHAT THIS IS

The single source of truth for the ZinStim brand and website. Everything below
is either decided, proposed, or open. Nothing gets built from a proposal.

**Brand:** ZinStim. Zinc + Stim. Fitness brand, Bangladesh-founded.
**Sells:** apparel, creatine and protein, accessories (watches, fitbands).
**Position:** a brand that sells confidence and tells a story. Product is the proof, not the pitch.
**Site style brief (from Kash):** breathing spacious minimalism, modern, cool font
effects, 3D introduced gradually.

---

## 1. OPERATING RULES

**R1 — Read before write.**
Read this file, then only the sub-file relevant to the task. Do not read
everything every time.

**R2 — Proposals live in files, decisions live in the log.**
Anything I invent is `PROPOSED`. It becomes `DECIDED` only when Kash says so,
and only then does it move into section 3 of this file. Never build off a
`PROPOSED` item.

**R3 — One file per layer. No new files without asking.**
The tree in section 2 is the whole structure. If something new is genuinely
needed, ask first and say what it replaces.

**R4 — Optimised output.**
Files are reference documents, not essays. Tables and specs over prose. No
restating what another file already says — link to it instead.

**R5 — Overwrite, don't accumulate.**
When a spec changes, edit the existing line. Do not append a new version below
the old one. History goes in the decision log, not in the spec.

**R6 — Previews are self-contained.**
`brand.css` and `nav.js` are canonical, but the HTML files inline a copy so they
open standalone — relative paths do not resolve in a preview pane. After editing
either canonical file, run `inline.py` to push the change into every
HTML file. Never hand-edit the inlined copies.

**R7 — Cleanup pass.**
At the end of a work session: empty `99-scratch/`, delete any file that no longer
has a job, confirm every `PROPOSED` item is either promoted or removed, update
"current state" below.

**R8 — No design defaults.**
Cream background + high-contrast serif + terracotta accent is the generic AI
house style. So is the rounded-card grid with identical shadows, and the
all-caps eyebrow label above every heading. If a choice could belong to any
brand, it does not belong to this one.

---

## 2. STRUCTURE

```
Zinstim/
├── ZINSTIM.md              you are here — rules, decisions, current state
├── theme.md                theme direction, nav naming, voice
├── tokens.md               the reasoning behind brand.css
├── components.md           nav capsule, three doors, divider
├── structure.md            pages, IA, section-by-section
├── brand.css               ← the system as code. single source of truth
├── nav.js                  nav behaviour, progressive enhancement
├── inline.py               build step — run after editing brand.css or nav.js
├── styleguide.html         live reference — open this to see everything
├── index.html              homepage
├── origin.html             the story page
├── elements.html           three doors — the range index
└── fuel.html               Fuel category page with lab panel
```

---

## 3. DECISION LOG

Only confirmed decisions. Newest at top.

| Date | Decision | Where it lives |
|---|---|---|
| 20 Sep | **Much smaller, and it stops stopping.** Kash on the full-bleed version: too big, too bled out, "why does it stop while moving". Both are the same mistake from two sides. SIZE: back to contained and well under where it started — `min(96vw, 56rem, 78vh x 128/114)`, which is 827x737 on a 1920x945 desktop (78% of the height) and 346x308 on a 360 phone (96% of the width) — found by walking up from 403x359 through 499x444 until it was as large as it can be and still be a whole mark with air around it; the next step up is bleeding again, which is where this started, whole, never cropped. At full bleed the thing you watched was a texture; at this size it is a mark. THE STOP: it was structural. The mark rose to the centre and the animation ENDED there, held by `forwards` for as long as the fetch, parse and paint took, and then a second animation started on the new page from rest. There is one animation now, `zs-mark-pass`, below the fold to off the top in .95s, and **the navigation happens inside it**: the leaving page hands over `Date.now()` in sessionStorage, the arriving page's head line turns that into a negative `animation-delay` before its first paint, and site.js then drives the pass off the wall clock every frame, closing any gap at up to 40ms a frame so a catch-up reads as speed rather than a flinch. That last part is not belt and braces: measured on a real HTTP navigation, the arriving document resolves its delay while parsing and does not render for another 60-140ms, then falls further behind because the pass is animating on the same main thread that is loading it — 89ms adrift a quarter second in, now 13ms. A frame that never rendered is a frame nobody saw, so resuming on time beats resuming where it froze. `will-change:transform` only while it travels, never at rest. Navigation now fires at .38s, as soon as the page itself is at nothing — what hides the swap is the page fade and a shared ground, not the mark, which is why the mark no longer needs to be the size of the screen. Sampled position across a real navigation: monotonic, no plateau, gone by ~1.05s. | `assets/css/brand.css` §transition, `assets/js/site.js`, the head line in all 7 pages |
| 19 Sep | **Refining the full-bleed transition — and finding two transitions running at once.** Filmed frame by frame, the rise was wrong in a way a contact sheet at four stops had hidden: the fill line sat BELOW the fold for the first 300ms, so the screen was a slab of flat metal and the carbon only caught up in the last 80ms. The fill is keyframed against the rise now — 91% of the way up the mark by 20% of the clock, all of it below the fold — so what reaches the screen is a band of bare metal riding the mark's top edge, closing as it lands (137px of a 1710px mark on a desktop, 70px of 780 on a phone). **The exit accelerates** rather than settling: `cubic-bezier(.5,0,.8,.3)` over .66s, because a thing leaving a screen speeds up; `is-arriving` clears at .78s to match. **The browser was running its own transition underneath ours.** `@view-transition{navigation:auto}` was opted into long ago so the nav would survive navigation; with the mark covering the screen at the swap there is nothing left for a cross-fade to do, and the two of them fought — Chrome aborted its own on every navigation, an uncaught `InvalidStateError` in each new document, which I had earlier written off as noise from my own test harness. It was not. Removed, with `view-transition-name:primary-nav`; the error is gone and the nav renders identically. **The next document is prefetched** on `pointerover` / `touchstart` (on a phone that is ~100ms before the tap lands), verified over a real HTTP server by the `[prefetch]` purpose header. A second click during a leave is swallowed instead of queueing a second navigation (click Wear, then Fuel 120ms later: lands on Wear). Hand-over trimmed 480 to 450ms, the end of the rise. One thing I checked and did NOT change: the art needs no warming — measured with scripts disabled, the stylesheet fetches it at load like any other background image, hidden element or not. | `assets/css/brand.css` §transition, `assets/js/site.js` |
| 19 Sep | **The mark goes full bleed.** Sized to the screen it still sat inside it — on a 1920 display, half the width was bare ground. Kash: full screen, full bleed, bleed out. So it is sized to COVER rather than fit — `max(100vw, 100vh x 128/114)`, whichever fill is larger — and runs past every edge at any window: 1920x945 gives 1920x1710 (the middle 55% of its height), a 360x780 phone gives 876x780 (the middle 41% of its width). The crop is the point of a bleed; what is left is still the diagonal, in carbon, moving, and the counters of the mark still read as the mark. The travel stopped being a number: `translateY(calc(50vh + 50%))` is half the screen plus half of itself, which is exactly off the edge at ANY size, so this cannot drift out of register again when the art or the window changes. **The art was replaced again** — at full bleed the 1024px cut was a 1.9x blow-up and the weave went soft, so it is now the source's own 1361px, every pixel the logo has, 109KB of WebP against 91KB (compared side by side at 1:1: the bevel highlight is a line again instead of a smear). Filmed at 1920x945 and 360x780; the real flow driven on a touch phone (leaving, pill, arriving, settled, pour still pinned at 72px); every page's scrollWidth still equals its viewport at 360; Firefox decodes 1361x1218, covers 1366x683, and runs every animation. | `assets/css/brand.css` §transition, `assets/img/zinstim-mark-lg.webp` |
| 19 Sep | **The mark goes full screen — the size the fluid was.** Kash: the Z should fill the screen as it goes up, the way the elixir did. It had been capped at 26rem, an object in the middle of a 1920 display. It is now `min(108vw, 96vh × 128/114)`: the height of the viewport on a desktop (927×826 at 1280×860) and the full width of a phone, bleeding past both edges (389×346 at 360) — a mark this square cannot be both without cropping the shape, and the shape is the point. The travel grew with it: it stands ~96vh tall, so clearing the screen takes 50vh + 48vh, and the keyframes run 118vh to −118vh, at .44s out and .76s back with the hand-over at .48s. **The art had to be replaced to survive the scale.** The masthead's is 128×114; at 927px that is a ninefold blow-up. The transition now uses a 1024px cut of the 2200px logo — and as a PNG that measured **1006KB**, which is not a thing to ship on every page, so it is WebP at **91KB**. Art and outline are each stretched to the same box rather than centred in it: their aspects differ by half a percent, which at this size is a six-pixel sliver of bare silhouette along one edge, against half a percent of stretch nobody can see. Filmed at 360 and 1280; the real flow driven on a touch phone; Firefox decodes the WebP (1024×916) and runs all four animations; pages at 1280 and 360 clean. | `assets/css/brand.css` §transition, `assets/img/zinstim-mark-lg.webp`, `assets/js/site.js` |
| 19 Sep | **The page transition is the mark, not a curtain of elixir.** Kash asked for the logo to go up instead of the cyan, and he is right on the brand's own terms: colour on this site IS the substance, and spending a screenful of it on every navigation is the one thing the 90/8/2 budget cannot afford, while the mark is the one shape the brand owns — so the move says the name instead of wiping the screen. Leaving, the page fades and lifts and the mark rises from below the fold to the middle of the screen, **filling with its carbon fibre as it climbs** (.40s) — the reveal the logo does under a cursor, at the size of the screen. Arriving, it carries on up and out behind the nav (.72s) while the new page settles in under it. Nothing here is a new colour: the silhouette is the Zinc Silver the masthead's mark is cut from, and what fills it is the carbon fibre art the mark already uses. Built on `body::before` (silhouette) and `body::after` (the art, clipped to the level it has filled to), both masked to the mark's outline inlined as a data URI, so no page carries markup for it and it never waits on a file; z-index 39 keeps it under the masthead's 40, so the mark passes behind the nav. The page itself is `main` — the footer sits inside it on every page — animated with `forwards`/`both`, so if a page's script never runs the animation still finishes and the page is left visible rather than blank. **Two faults caught on film:** the silhouette never showed, because I wrote `.night body::before` — which means a body inside a night element, and the class is *on* the body — so it stayed Obsidian on Obsidian and only the carbon showed; and the fill used `--ease-out`, which front-loads its travel, so the mark was 85% full 140ms in. It is `body.night::before` now and the fill climbs on the mark's own curve. Filmed at 360 and 1280 by seeking every animation to exact times; the real flow driven on a touch phone (sheet → Wear → arrive → mark → home → mark on home → Back); a plain click raises no exceptions; the pour still pins after `main` has been animated (stage at 72px under the masthead); Firefox identical. | `assets/css/brand.css` §transition, `assets/js/site.js`, `styleguide.html` #motion |
| 19 Sep | **A page transition: the liquid carries the reader between pages, under the nav.** Kash: what he selected was not visible at the top, and choosing a page or tapping the mark produced no animation at all. The first was the phone pill's black-on-black bug, already fixed locally — his screenshot was from the deployed site at 03:36, before the fix and before any redeploy. The second was real: the site is separate pages, so every choice swapped the screen in one frame and never registered, and the mark's fill only ever ran under a desktop hover. Now the elixir rises from the bottom over the page being left (.38s, ease-in) with the pour's wave on its crest, the next page opens already covered and the liquid carries on up and out (.72s, `--ease-move`) with a wave on its tail. It runs **under the nav** (z-index 39 against the masthead's 40, and nothing on any page goes above 10), so the choice stays on screen the whole way across: the phone's pill switches to the tapped page's name as the liquid rises, and the mark fills with its carbon fibre as it goes and drains as the new page arrives. The mark tapped on its own page fills and glides to the top instead of reloading. Mechanics: `site.js` intercepts clicks on links to the site's own pages (never new tabs, modified clicks, same-page anchors, downloads, other sites or mail links), adds `.is-leaving`, leaves a note in sessionStorage and navigates after .38s; one line in every page's head reads the note and adds `.is-arriving` before the first paint, so a new page never flashes up uncovered; the page's own load moment waits .28s so the headline rises as it is uncovered rather than beneath the liquid; Back and Forward, which restore a page exactly as it was left, are uncovered on `pageshow`. **Tuned on film, not by eye:** the first arrival used `--ease-out`, which does 80% of the travel in 150ms, so the page popped out from under the liquid — `--ease-move` holds a beat and then sweeps; and with a wave on the crest only, arriving read as a blind going up, because the edge the reader watches on arrival is the trailing one — it has its own wave now. Driven on a touch phone at 360 (sheet → Wear → arrive → mark → home → mark on home → Back) and in Firefox, where a local `file://` page may keep each file's storage apart, in which case the arrival simply does not play; on the deployed site it does everywhere. Reduced motion: pages simply change. | `assets/css/brand.css` §transition, `assets/js/site.js`, `assets/js/nav.js`, every page's head, `styleguide.html` #motion |
| 19 Sep | **Elements stops being a page; the phone gets room and a nav you can see.** Kash, on a real phone: it is too squeezed, the nav vanishes once you choose a page, and — asked — how do you even reach Elements on a desktop, and do we need it? **Elements is retired as a destination.** The page was a title and three full-screen links to Wear, Fuel and Instruments, each captioned with the lede of the page it opens — the nav's group and the homepage doors, a third time. The desktop nav had always treated the word as the group's *handle*: the capsule opens on the way to the word and hides it (`pointer-events:none`), so the page was never reachable from a desktop at all. So the word is now what the nav already made it. On a desktop a click or Enter on it opens the group (Enter lands on Wear); on a phone it is a heading over the three in the sheet; the footers drop it; the label's href falls back to the homepage chooser (`#elements`) for when no script runs. `elements.html` is left on disk, unlinked — one step to restore. **The nav on a phone was black on black.** The desktop pill had been inverted for Night; the phone's never was, so its page name and its dot were Obsidian on the Obsidian masthead and a reader saw a teal dot and two bars. Its words are Zinc Wash now, its dot a Zinc Wash disc, and the sheet the dot opens into is the same Zinc Wash — the dot is seen to become the sheet — with the page you are on in Elixir. **Squeezed, measured on the widths real phones have** (360 and 384; the first pass tested 390): the footer had no side gutter anywhere — `padding:X 0` on `.foot` overrode the shell's sides — so it ran from the screen's edge and on a narrow phone past it, which an overflow check cannot see because a page cannot scroll left; it keeps the gutter now and stacks below 641px. Fuel was 23px wider than a 360px screen — not the tub's wave (an SVG's inner geometry never widens a page; it measured 383 before and after clipping it, so that change was reverted) but the lab table, whose four columns cannot go under 359px in a 328px column. Rather than squeeze it, its rows stack below 481px — test and status, then result and specification — with explicit table roles so the display change cannot strip its semantics; the style guide's spec tables stack the same way below 641px. **The sign-off gets room:** below 600px the wordmark is set on two lines, ZIN over STIM — zinc and stimulus, the two halves the name is made of — fitted to the width by measuring it, in a band nearly as tall as the screen is wide (342×294 at 390, where it had been 342×89), with a stretch of dark above and below. Inner-page heroes are taller on phones so the headline and the cut are not packed together. Every page at 360, 384 and 390 as a touch device and at 1280: no errors, zero horizontal overflow; last pass's touch interactions re-run and passing; Firefox clean. | `assets/js/nav.js`, `dotmark.js`, `assets/css/brand.css` §nav, §foot, §phones, `fuel.html`, every page's footer, `styleguide.html` |
| 19 Sep | **The phone pass: every hover, cursor and scroll feature given the phone's own equivalent, and the bloom made to leave with its headline.** Kash asked whether the red and green blobs should follow the reader down. **No** — the bloom is one glow behind one line; lit everywhere it points at nothing, and the sections below were asked to be flat black. What was wrong is how it left: at the speed of the text, like a sticker pushed off the page. It now drifts down against the scroll and dims as the hero goes (`--exit` from `site.js`; opacity 1 → .67 → .27 measured at 0, half and nearly out), and the hero clips it (`overflow:clip`) so the drift cannot carry it into the black below. **Then the phone, planned per feature before any code,** from a survey of all seven pages as a touch device (hover:none, pointer:coarse), audited for targets under 44px, text under 11px and overflow. The rule: a phone has no resting pointer, so each hover is replaced by what it was *for* — reveal-on-look takes the middle line (already there for pieces, kit, Elements and the Fuel tub), press feedback takes `:active`, the cursor's toys take the finger, ornamental fills fire on arrival or on a tap. **Gaps closed:** the homepage doors swapped faces on touch but their cards only answered `mouseenter` — they now come in on the line, the last word to cross it holds until another does, and the section clears when it leaves; the card field itself had been `display:none` below 861px, so it was re-composed for the phone (the section is 408px at 390, so whenever a word is on the line every card is on screen), cards in the gaps beside the narrow words, the unchosen words dimmed. **The wordmark** was mouse-only and a still picture on phones: pointer events, a fingertip-sized radius, `touch-action:pan-y` so a sideways rub scatters and a vertical drag scrolls, a pour-together arrival each time it comes well into view, a taller denser word below 600px (it was a 54px word on a 6px grid), and it sleeps once every dot is home instead of redrawing a still frame sixty times a second. **Wear** gets a sideways swipe between pieces (48px, mostly sideways, inside 700ms; a vertical drag scrolled 413px and turned nothing). Titles pour on a tap. The kit's line said "Hover one" to phones; gesture copy is now `.on-point`/`.on-touch`. Footer, Wear's tabs and the style guide's contents are 44×44 (they were 28, 35 and 21px tall); mono captions have an 11px floor; fields are ≥16px so iOS stops zooming into them. **Two faults found on the way, fixed everywhere:** the phone stripe rule also matched the bloom div and painted a second set of stripes over its glow, so the red and green had never shown on a phone — invisible until the bloom began to drift and the copy peeled away as a ghost; and the Wear and Fuel door cards were `#161618` on Night, 1.08:1 — they now carry their light-page materials lit for the dark (lid silver 5.34:1, Elixir 3.03:1, beside Instruments' blue at 3.40:1). Every new behaviour driven with real touch events at 390; Origin, Fuel and Elements walked by scrolling as a thumb does; seven pages at 1280 and 390 with no errors and zero horizontal overflow. | `assets/js/site.js`, `dotmark.js`, `assets/css/brand.css` §surfaces, §doors, §phones, `index.html`, `styleguide.html` #phones |
| 18 Sep | **The Wear viewer on Night: its interactions were all still running — there was just nothing to see them by.** Kash, against the light build: the structure was right before, the colours are not doing it a favour, and on Night it is too dark to notice what is selected. The viewer's three interactions are the nav's morph at three scales — the tab's dot opens into a pill, the ground opens out of a circle into a block, the action opens from a square into a pill — and all three were drawn *darker* than the page so they would stand off white. On Obsidian that is ink on ink: the tab pill and the action square were Obsidian on Obsidian, and the block had been made transparent in an earlier pass. So they now invert the way the nav's morph already does on Night, on one rule — **whatever you have chosen, and whatever you can press, is Zinc Wash**: the selected tab is a Zinc Wash pill with Obsidian words and an Elixir dot (16.4:1), the action is a Zinc Wash square with an Obsidian arrow that opens into a pill with the next piece's name (16.4:1 against the page, 5.4:1 against the block), and the selected view is a lit Zinc Wash plate with its drawing in ink. The resting views get an edge you can see (a 16% Zinc Silver hairline) and a brighter one under the cursor, so the three states — rest, hover, chosen — are three different things. **The block is the substance, not a selection**, so it is the one thing that does not go white: Elixir, the colour of the pour's surface, easing toward Elixir Deep with depth as the flood does. Measured against the page: Elixir Void, what it had been, 1.23:1 — a dead grey rectangle; Elixir Deep 1.91:1; **Elixir 3.03:1**, the one that reads as a ground while the piece still reads on it at 5.41:1; Elixir Lit 7.31:1 but the piece falls to 1.94:1 on it. The small type moved from Graphite, the light page's secondary grey, 4.19:1 on Obsidian, to Muted, 5.65:1. One timing fix: the selected tab's words darkened while its pill was still a circle behind the dot, so for a beat they were dark words on a dark page — they now wait .3s for the pill to grow under them. The viewer's night rules had been spread across three correction passes; they are one block now. Every state driven and captured — rest, tab mid-morph and settled, view switch, hover on the action, hover on a view — at 390, 1280 and 1920; the light build computes exactly as before; Firefox resolves the block's `color-mix()`. Seven pages: no console errors, zero horizontal overflow. | `assets/css/brand.css` §surfaces (the viewer on Night) |
| 18 Sep | **The stream moved behind the jar and behind the flood — so each end is hidden by what it is leaving or entering.** Kash, zoomed in: the liquid clips at the lip and again where it meets the rising water, and it should come from behind the bottle on the z axis. Right, and it is the general answer rather than a patch: the stream was drawn on the same layer as the flood, in front of the jar, so both of its ends were bare cuts — a straight cross-section at the lip that met the tilted collar at an angle and left a wedge of gap, and a flat end sitting on top of the pool. Now there are four layers, back to front — **stream, jar, flood, claim** — the stream in its own SVG (`.pour__jet`). Its source moved *inside* the collar (`.pour__mouth` 96.5% → 88.9%, 20 units short of the face), so what the reader sees is liquid coming out from under the lip; and it ends *under* the surface (`landY = surface + clamp(H×.045, 18, 40)`), so the flood covers the join and the stream goes into the liquid. That also retired the opacity fade — once both ends are occluded, the rising surface simply covers the stream from below. **It leaves along the jar's axis**, not horizontally: the parabola's tangent at t=0 is the jar's own tilt (`va = runX·tan(r)`, gravity supplying the rest), so the first cross-section runs parallel to the collar face. **Nothing leaves the lip until the jar is past level** — it crosses 0° at about p .163, and a drop had been forming at .13 on a jar still tilted up — so the pour starts at .17, and IMPACT moves to .32. **The let-go now happens in the order it would:** the flow ebbs to half (.58–.66), the last of it drops off the lip (.62–.69) *while the jar is still in place over it*, and only then does the jar right itself (.66–.73) and leave. Before, it untipped from .64 and swung the collar away from a stream that was still attached, leaving the stream's top hanging where the lip had been. Two faults found at magnification, not reported: the tail's round cap was sized as a share of the path, so on the ebbing thread it drew out into a needle — it is now as long as the stream is wide; and samples were packed toward the head for the bead, which left the cap a single point, so they are now on cosine spacing, dense at both ends. Checked at 640, 1280 and 1920, on both grounds. Firefox scrub: no errors, layers at 0/1/2. Seven pages: no console errors, zero horizontal overflow. | `index.html`, `assets/css/brand.css` §pour, `assets/js/site.js` §pour |
| 18 Sep | **The pour runs on one clock now, and every beat is caused by the one before it.** Kash: the starting shape was wrong, there were gaps, and particles were floating. All three had one cause — the beats were timed independently, against numbers typed separately. The pool opened at p .28 and the chips flew at .26, but the column did not arrive until .36, so the strike happened in mid-air with nothing under it and the chips hung in the gap with nothing to have come from. Now there is a single constant, **`IMPACT = .30`**, and the rest is placed against it: the column falls from .13 to IMPACT on an ease-in (it is accelerating), the pool spreads from IMPACT, and the chips are gated on **`landed`** — derived from the column's own head — so they cannot precede it. They are thrown from the point of impact and pulled back down (`+ throwP² × 34`), where before they opened to ±420px on a fan of their own and hung there. **The starting shape:** the bead was a fixed .11 of the column, which was longer than the whole stub for the first third of the fall, so what left the lip was all tip profile — a lump. It is now a share of the column that exists, and its profile is a real drop: a thread swelling on a smoothstep into a circular bulb, both joins at zero slope, where the old sine-times-quartic peaked, pinched and closed, and the pinch was the notch. Sampled denser toward the head (`1 − (1−f)^1.7`), since at even spacing the bulb got three points. **Three later faults found by rendering, not reported:** the pool's front was still out in the frame when the level began to climb, so it stood up as a vertical wall — the spread now finishes at .47, before the fill has moved; the column faded over 90px and was half gone at p .50 while the jar was still tipped, because it lands on the mound's peak right under the lip — 45px now; and once the pour stopped, the slug that let go of the lip had a flat top and read as a sliver, so it now gets a quarter-circle cap once the tail has left. Once the flood passes the lip the column ends, rather than solving a parabola against a target above its own origin. Full scrub in Firefox: no errors. Seven pages at 1280: no console errors, zero horizontal overflow. | `assets/js/site.js` §pour |
| 18 Sep | **The pour section rebuilt: the jar was sitting on the words, the blur was not running at all, and the stream had its physics inverted.** Kash, comparing against the uncommitted build: the flow looks wrong and the code is overlapping. Three faults, all mine, all measured rather than argued. **The jar was 52px on top of the claim.** The vessel is sized by width with `height:auto`, so its viewBox aspect decides how far down the page it hangs; taking it from 280×120 to 230×130 to make it chunkier also made it render 323px tall instead of 286px. Back to **270×120** with the tub redrawn inside it: 347×290 rendered against the reference's 344×286, and the gap from the jar's foot to the claim's head is **+17px** at the tip and **+11px** at the landing, against the reference's +21 and +16. **The blur had not run since the arrival pass** — swapping in `--a` replaced the `--c` setter instead of joining it, so coverage was frozen at 1 and the claim sat fully opaque and sharp from the first frame. Caught by reading the computed variable, not the source. **The stream had all three of its physics backwards.** It was a bezier with a hand-placed control point, it got *wider* as it fell, and it ended in a flat cut. Liquid leaving a lip is a **parabola** — x linear in time, y quadratic — it **necks**, because continuity puts the same volume through every section and it is faster the further it has fallen (`w = w0/sqrt(1 + 4.6s²)`), and while it is airborne it ends in a **bead**, because surface tension will not leave a cut edge. It also lands on the live surface at the landing column rather than on the floor, so the fall shortens as the pool fills, the way it does into a filling glass. The facets came out of it: zinc cleaves and the section edges and strike chips say so, but a falling liquid does not, and faceting it read as a torn scribble hanging off the lip. Also fixed: the mound decays against the **raw** fill rather than the dwelled one — warped, the dwell held it up until the surface reached the claim and the line was read across a lump — and the fill starts at .40 rather than .34 so the pool spreads before it rises instead of the stream landing on a slab. Crossing still **331px**. Seven pages: no console errors, zero horizontal overflow. | `index.html`, `assets/css/brand.css` §pour, `assets/js/site.js` |
| 18 Sep | **The waterline decides the colour, the jar replaces the bottle, and the pour cleaves.** Kash, comparing the two grounds: should the claim be black or white? Neither — it is the wrong question, because the line crosses two grounds in one shot, so a single colour has to be wrong in one of them. Measured against every ground the shot passes through: black is 16.4:1 on Zinc Wash but **1.2:1** at the flood's depth, wash is 16.4:1 on Obsidian and **13.3:1** at that same depth. Black drowns as the liquid deepens and wash strengthens, so **above the waterline the type belongs to the page and below it the type belongs to the liquid, and the liquid's type is always Zinc Wash** — one rule, both grounds. Cut with `background-clip:text` against `--wl`, which the controller writes from the **lowest** point of the surface across the claim's own width, not the surface at its middle: the surface is a wave and the split is a straight line, so measuring at the middle puts wash type on dry ground out at the last word. The glow is gone — the reference is cut paper and cut paper has no light in it; the liquid reveals the line by covering it. **The blur comes back and the liquid owns it — on one element, after a two-layer version failed.** The per-region attempt stacked a blurred copy over a sharp one and masked them against each other at `--wl`. It was wrong for a reason worth keeping: **filter runs before mask**, so cutting an already-blurred copy at a hard line leaves the smeared tops of the next line standing above the surface, and Kash read the claim as double-struck. One element, blurred by coverage the way the reference does it, has no seam to smear. What the reference did not have is the floor and the colour: opacity bottoms out at **.5** rather than 0 — 3.52:1 on Zinc Wash, 4.65:1 on Obsidian, both clear of the 3:1 floor this size of type sits under — and the colour is still cut at the waterline. **The crossing was also too fast to see** (about 130px of scroll) because the fill ran through `easeInOut`, whose quickest stretch is the middle and the middle is the part that has to be slow. The fill is now warped to linger across the claim's measured band — decelerate in, hold, accelerate away — which puts the crossing at **361px**, verified by scanning the section at 30px steps. **The vessel** is no longer a bottle: it is the Zn 30 tub, chamfered rather than rounded because zinc cleaves, with the collar reaching the mouth the stream actually leaves from. It stays black with a silver collar on both grounds — what changes is which is solid and which is drawn in line, since a black tub vanishes on Obsidian and a silver collar vanishes on Zinc Wash; outlining the dark one is what `wear.html` already does with the tee. **The column cleaves instead of pouring**: coarse facets stepped off the spine on two co-prime strides, fixed per facet so it cannot boil frame to frame, and the strike throws eight chips off the live surface. From the Powerpuff Girls opening Kash sent — the Professor tipping the carton, and the shards. Checked in Chrome and in Firefox, where the split computes at 82.6% → 28.4% across the crossing. | `index.html`, `assets/css/brand.css` §pour, `assets/js/site.js`, `styleguide.html` §motion |
| 18 Sep | **Three corrections, one of them self-inflicted.** Kash: the nav stopped sticking, the bottom still was not fully dark, and the fracture under Origin's headline was a pointless grey band. **The nav** was mine: `.night .masthead{position:relative}` — written to lift content above the viewport-fixed bloom — silently overrode `.masthead{position:sticky;top:0}`. The bloom it existed for was deleted a round earlier, so the rule went with it; `position:sticky`, `top:0`, `z-index:40` verified on all six pages. **The fractures**: a cleave breaks between two grounds, and a night page has only one, so painting a lighter shard across it was inventing a break that is not there — which is exactly what read as grey noise under a headline that already carries the cut. They now collapse on night and return only where Night actually meets Day: Fuel's lab panel, where the break is real and prints as crisp as it does on a light page. **The pour claim** answers Kash's question — he was right. `.pour__claim` was `--zinc-wash` at `opacity:var(--c)`, where `--c` is how far the liquid covers it: on a light stage that is white type on white until the flood arrives, and the copy is the section's own heading. It now rests in `--ink` at a 0.34 floor and turns wash only once it is under — the same rule as the pours, that motion may bring copy forward but may not carry it. Bottoms proved by pixel rather than by eye: the last 900px of every page sample flat `#0D0D0D` at both edges, the only exceptions being the marquee's own letterforms and Fuel's Day panel. Seven pages at 1280: no console errors, zero horizontal overflow. | `assets/css/brand.css` §surfaces, §pour |
| 17 Sep | **The night pass, corrected: audited instead of patched.** Kash, on the first migration: it looked broken, the cut was missing, the blooms did not follow, and the colours did not agree. All fair, and the cause was the same each time — the ground flipped but components drawn for a white page did not. Fixed in one audit rather than screenshot by screenshot. **The colour clash:** page titles still poured Flame Teal, from the light system where teal was the accent; under Deep Carbon teal is a signal, so a teal title fought the cut, the bloom and the live dots at once. Titles now pour `--type-accent` (#E87A82) with a crimson wave — one family per page. **Grounds that stayed white:** `.shout` (the UNLEASH band, a white slab mid-page), `.kit__item`, `.door-card`, `.plate`. **Inks that stayed Obsidian:** the Fuel tub's outline (invisible), kit spec bolds, view-tab hover, field focus border, quote attribution, and the viewer's upper half of the split drawing. **The product field:** a whole panel of Elixir Deep was more colour than the system spends and reading as a grey rectangle once neutralised; it is now `--elixir-void`, 1.2:1 on Obsidian — a body without light. **Nav:** dot-to-pill was Obsidian on Obsidian, so the interaction ran invisibly; the pill inverts to Zinc Wash with Obsidian words and Elixir for the current page (Flame Teal on Zinc Wash is 1.4:1). **Pours:** the dry state was a pale grey tuned for white and read as broken text on black; lifted to about 8:1, so a word is visible before it fills — motion may not carry information. ~~**The bloom** is now fixed to the viewport, so the crimson and teal follow the reader.~~ *(Reverted 17 Sep: Kash wanted the section flat black, no blobs. The bloom is the hero's alone.)* Rechecked at 1280: seven pages, no console errors, zero horizontal overflow. | `assets/css/brand.css` §surfaces + night corrections |
| 17 Sep | **Deep Carbon is decided, and the site is being built in it, page by page.** Kash picked **cut in both**: the base is Deep carbon untouched — the crimson bloom, the crimson button and its halo, teal for live states — with Taskin's diagonal added, Lid Silver `#80878D` leading over Oxblood `#671E24`. Built as a surface layer rather than a repaint: `:root` now carries `--surface / --ink / --ink-2 / --rule-c / --action / --action-label / --action-fill / --action-fill-label / --live / --type-accent / --bloom`, **.night** and **.day** set them, and nesting works (a `.day` inside a `.night` gets the light values back) — the same pattern the colour lab proved. Every existing dark-context rule was rewritten from `.band--dark X` to `:is(.band--dark,.night) X`, 41 of them, so the whole component library answered to night pages without being repainted. The cut is `.has-cut` + `.cut` + `.cut--bloom`, 62° from the horizontal, one set per page, cropped to a single band below 860px so nothing sits behind the copy. Fractures collapse on a night page except the elixir wedge and the two that border a `.day` band, where they now break the right way round. Night chrome added for the pieces that painted their own light ground: masthead, pour stage, manifesto, product plates (`#161618` with Zinc Silver drawings), rules, footer links, focus rings and selection; `dotmark.js` follows `.night` as well as `.band--dark`. **Pages done:** styleguide (new §Deep Carbon documenting both surfaces, the cut's geometry, the token table and the four rules), index (night, the cut in the hero, theme-color flipped), elements, wear, fuel (its lab panel is the page's Day surface — the proof), instruments, origin. Checked at 1280 and 1440: no console errors, zero horizontal overflow on any page. | `assets/css/brand.css` §surfaces, every page, `dotmark.js`, `styleguide.html` #surfaces |
| 17 Sep | **Motion build sheet, so the fills can be rebuilt in After Effects.** Every number read off `brand.css` rather than re-invented: the wave (period 50 units, ±3.5 amplitude, baseline y=7, drifting exactly two periods — 100 units — per 1.6s linear, which is the only distance that loops without a seam); the mark (1024×912 comp at 8×, silhouette as one matte, wave body as the other, level 0 → **1.12** in 0.75s, and why 1.12 rather than 1); the button (blob = max(2.4 × width, 224px) square at 0.42 corner radius, top edge +118% → −46% of button height in 0.75s, 360° per 5s, label flipping 0.3s starting 0.2s in); liquid type (two identical word layers, 1.32em liquid box, rest level **0.5** because at 152px the waterline then sits 51% down the ink, hover 0.8s, pour-in 2.2s after 0.55s); the vessel (0.10 → 0.94, 1.05s, never 1 or the drawing disappears); the flood (280% square at 0.43 radius, +104% → −18%, 1.1s, 360° per 12s). Both easings converted to After Effects influence values, plus a paste-in expression that solves the same cubic-bezier the browser does. Export, Lottie caveats and a pre-send checklist included. | `docs/motion-build.html`, `docs/motion-build.pdf`, artifact: motion build sheet |
| 17 Sep | **Three cuts, for Kash to choose between.** Correction to the row below: Carbon steel changed too much. What Kash asked for is **Deep carbon exactly as it is** — the crimson bloom behind the headline, the crimson button with its halo, teal for live states — with Taskin's diagonal added on top, and only the stripe colour in question. Three tabs, identical but for that: **Cut in silver** (`#80878D` over `#494E51`, the jar lid's own metal, stripe at 5.3:1), **Cut in crimson** (`#AE4047` over `#480F15`, the mark's red, 3.4:1) and **Cut in both** (silver leading, oxblood `#671E24` behind). Recommended: **Cut in both** — it is the jar itself, silver lid and crimson mark on a black body, and the two values keep the diagonal reading as two materials rather than one thick band. Silver alone is the calmest and the most instrument-like, but at 5.3:1 the stripe is the brightest thing on the page; crimson alone has the most energy and sits closest to the aisle's signature, with red doing three jobs at once. The lab opens on Cut in both; Deep carbon is relabelled the base, Deep Zinc stays as the quiet one. | `lab/colour-lab.html` |
| 17 Sep | **Carbon steel replaces Deep Zinc: red acts, but only in the dark — and the book now shows every state.** Kash: he prefers the red-led direction and likes Taskin's diagonal stripes, with the stripes in the jar's silver rather than red. Re-measured, and the objection that killed red-led was a *light-surface* problem: on Day the failure colour `#B0332B` sits **0.033** from the mark's crimson, but on Night failure is `#FF7A6B`, **0.208** away — two steps lighter and plainly different. So crimson leads the actions on Night (button fill `#AE4047`, Zinc Wash label at **4.9:1**, filling to `#F29BA1` with an Obsidian label at **9.2:1**), and on Day nothing is crimson but the mark, because there red still means failure. The stripes are cut in **Lid Silver `#80878D`** — the lid measured L 62% at hue 70–86° in the render, set in the brand's cool band — with `#494E51` as its shadow; that is what stops black-and-red reading as the supplement aisle. Four rules carry the risk: one crimson action per screen, crimson never carries small text (3.35:1), every error keeps a word, and the mark's reveal stays the only place red is material rather than action. Ratio restated **90 / 7 / 3**. The lab gains a sixth tab, **Carbon steel — the pick**, now its default; Deep Zinc stays as "the quiet one" (one token change back) and Deep carbon is marked superseded. The book adds **states and interactions** — primary and secondary buttons, the mark, navigation, product, category and field, each drawn in rest, hover, focus, pressed, pending, error, disabled and touch, with a timing table — because paper cannot hover and the team kept having to ask. PDF reprinted: 23 A4 pages, no blank pages. | `lab/colour-lab.html`, `docs/brand-book.html`, `docs/brand-book.pdf` |
| 17 Sep | ~~**Mission written, the decision named Deep Zinc, and the book prints.**~~ *(Deep Zinc superseded by Carbon steel the same day — see above. The mission, the two lines and the standards table all still stand.)* Mission: *We make the small correct things training actually runs on, and we publish the proof* — the point being that in this market a claim is free and proof is not, so every product states grams, batch, date and the lab. Two lines the team can repeat instead of explaining the system: *Everything fills because a body fills: blood moving, a dose landing, a level rising toward ready*, and *The mark is red because muscle is, and woven in carbon because that is what repetition makes — many small fibres, one strong object.* Kash's reason for the fluid, recorded as his: it reads as blood pumping, as something changing, as getting ready. The colour lab gains a sixth direction, **Deep Zinc**, which is now its default and its conclusion (Obsidian ground, Zinc Silver mark and type, Flame Teal as the only live colour, a teal-rimmed button that fills with Flame Teal, categories in silver / elixir lit / info lit, no glow); Deep carbon is relabelled rejected and its note carries the five reasons. Every pair in the new direction measures clear: body 16.4:1, headline 11.4:1, filled button label 11.4:1, category on flood 8.6:1. The book adds the mission, the two lines, and a **standards and sizes** table — which colour may do which job, at what minimum size, with the figure that permits it — plus the 8% ceiling stated as "no more than three teal things visible at once, one button per page". Print rules added (each section starts a page, tables stop scrolling and fit), so `docs/brand-book.pdf` is 24 A4 pages with page numbers. | `lab/colour-lab.html`, `docs/brand-book.html`, `docs/brand-book.pdf` |
| 17 Sep | **The moodboard is measured, and product photography has two setups.** Kash saved the product render as `ref6` and asked for the moodboard and the style to be set from it. Sampled: the five original references run **56–84% of frame below L 30%** with mean chroma 0.001–0.014, and `ref6` runs the other way — **7% dark, 66% above L 75%**, every surface warm at hue 70–97°. Its counter `#E3DDD2` is **0.047** from Zinc Wash and **0.042** from Bone, the cream this brand dropped for being warm; the lid `#8C8479` is **0.278** from Zinc Silver and nearer Graphite; the jar body reads `#43443E`, **L 38%** against Obsidian's 16%; the mark's lit face `#713235` holds the hue (19° against 20°) but lands two steps dark, as oxblood. So: **the object is right, the room is not.** Two setups are specced with in-camera targets — **Night** (ground `#0F1214`, slab `#2B2E30`, one hard key, no fill, at least half the frame below L 30%) for brand images, **Day** (ground `#D8DFE4`, wall `#BFC5C9`, concrete `#82878A`, cool north light) for spec images — under one rule: every surface stays in the cool band (hue 200–260°) or under 0.010 chroma, never 60–100° above 0.012. Two facts the sampling settled: the elixir was **read off `ref4`'s car**, whose teal measures hue **172°**, two degrees from Elixir Lit; and 90/8/2 is what that same frame already does (red 1.8% of pixels, teal 0.6%). Pack spec written with it: matte body, cool metal lid, `Zn 30` struck once as the pack's only naming moment (so the floor of Zn tiles goes), the mark flat in every digital file, **ZinStim** in Archivo rather than the render's rounded italic, one teal line for the dose. | `docs/brand-book.html` §09, `assets/img/moodboard/ref6.jpeg` |
| 17 Sep | **Red is the material the mark is made of, not the brand's colour — and the brand book says so.** Kash, after the colour lab: red carries negative connotations, it is aggressive, and it does not flow with the rest. Agreed, and measured. The crimson-led direction (Deep carbon, the lab's own synthesis) is **rejected**, for five reasons in this order: it contradicts an argument built on the unnoticed correct input; the system already spent red on failure, and `--fail #B0332B` sits **0.033** from the mark's crimson in OKLab, so an action colour and an error would read alike; black-and-red is the category uniform (ASRV, ANZO, the supplement aisle); crimson on Obsidian is **3.35:1, APCA Lc 23**, so it cannot carry a word on our own ground; and under deuteranopia our pass and fail land **0.076** apart while crimson against the elixir core lands **0.091**, so no state may ride on hue. The settled motif is **Night, metal, one live colour, and the mark's own red** at **90 / 8 / 2**, on two surfaces: **Night (Obsidian)** for brand pages, packaging and social, **Day (Zinc Wash)** for size charts, lab panels, dose tables and policy — the brand argues in the dark and proves itself in the light, which also suits mid-range Android in Dhaka daylight. Red is allowed only as the artwork the reveal uncovers, the tab icon, a physical finish (one crimson thread, anodising, print on black) and photography of the object; never a button, band, headline, badge or chart. Teal holds because it is the mood board's one colour event, it sits five degrees off crimson's true opposite (356° against 171°), it is what a mineral in solution looks like, and it is a neighbour of the flag's green (**0.023** in OKLab) without ever being a flag. Three smaller rules settled with it: the Elixir **core** may carry small type on Zinc Wash (**5.4:1**) — the "never on light" rule was written for Elixir Lit and above (2.2:1 and worse); uppercase is allowed in Space Mono at small sizes only; and the lab's crimson glow behind type is **not** adopted, so surfaces stay flat. Written up as a brand book for the team — name, mark construction (blade 45°, wedge 26°, 9:8, clear space 0.25 H), colour with every figure measured, type, the fluid motion system and why it is fluid, voice, pictures, applications and what is fixed versus open. | `docs/brand-book.html`, `docs/tokens.md`, artifact: brand book |
| 16 Sep | **Phones get the same site, not a reduced one.** Kash: fix responsiveness and think through every interaction on a phone, starting with the nav. **The nav** stacked its links into three rows of a sticky header (at 474×237 the header was 155px of a 237px screen) and hid Elements entirely. Below 861px it is now one 72px row: the page you are on with its teal dot, beside the nav's Obsidian dot, and tapping grows that dot into a sheet of every page — a clip-path circle centred on the dot, the desktop's dot-to-pill turned downward — with the pages rising in and the current one in Flame Teal. Built by `nav.js` from the links already in the markup, so without the script the plain list still works. While the sheet is open the mark reveals its carbon fibre, since a phone has no hover to reveal it with. **Hover had no phone equivalent** for the homepage pieces, door words, kit cards, the Elements words or the Fuel tub (a phone saw static half-fills or nothing). On touch devices the thing crossing the middle of the screen now takes its hover state (`.is-pointed`, `site.js` touch, `brand.css` §touch) — the reader's scroll stands in for the cursor, the rule Origin already uses. The middle is a line, not a band: at 16% of the screen the three stacked door words all swapped at once (measured). Buttons fill while pressed, and Wear's next action shows its label on touch. **Short screens** (a phone held sideways) no longer pin Origin's thesis and gesture, which ran out of stage at 390px tall. Checked with real taps in Chrome's touch emulation at 360×640, 390×844 and 844×390 on every page, a real click in Firefox, each touch state probed at mid-screen, and the desktop nav unchanged at 1440. Kash's zoomed Edge screenshot, with the text pushed off to the right, was pinch-zoom on the desktop layout rather than a layout bug: the logo sat at its desktop position ×4 and the join text at the desktop column's left edge ×4. | `assets/js/nav.js`, `site.js` touch, `brand.css` §nav — small screens, §touch, §origin, `styleguide.html` #phones |
| 16 Sep | **The carbon-fibre reveal is back; the tab icon stays red.** Kash, with a screen recording of the reveal: “fix the home icon hover effect you had before — reveal the red carbon fibre as I hover over it.” The red-at-rest mark and the liquid home button are undone and the reveal is restored exactly — black at rest, the carbon fibre rising in under the cursor, the grey disc — with every page's mark compared character for character against the copy saved before the change. The one thing kept from the change in between is the red tab icon: the tab shows the brand's colour, the nav mark reveals it. | every page, `brand.css` §nav and §the mark, `styleguide.html` #mark, `docs/tokens.md` |
| 16 Sep | **Origin reads like a pour: below the hero, the reader's scroll is the substance.** Kash asked for Origin to be reworked — research fluid motion, give each section a better representation in the same style, think like marketing, break nothing. Below the hero the page was five sections built the same way (a heading, small grey paragraphs, the right half empty), so it read as a document after a poster. Every word of copy is kept; each section gets its own shape, and all of them move by one rule: `site.js` (scrub) writes each section's scroll progress as `--p`, and CSS turns it into a level. **The thesis** pins and fills with ink word by word as it is read; the quiet line after it just arrives. **What we make** sets the four things hollow at display size, and each pours full as it is reached; “underneath.” takes the elixir. **The name** becomes the zinc tile (30, Zn, 65.38) whose symbol holds a two-gram teal sliver; what zinc runs pours in teal, drains line by line when the copy says what happens without it, and comes back with the last line — the tile rides beside the copy so the drain is watched, not inferred. **The gesture**: UNLEASH is set the way the category sells it, widest, heaviest and full of ink, and collapses (width 125→62, weight 900→200, ink draining to an outline) while “small correct things” fills. **The refusals** are struck through in reading order as they are reached. **The commitments** become a batch line the elixir runs along, filling each station, followed by the page's one mid-page ask, *Join the list*, placed at the proof. Research, and the measurement that shaped it: 2026 write-ups call scroll-driven CSS baseline, but Firefox 156 — Kash's browser — returns false for `animation-timeline`, so this is one script writing one number (`linear()`, `@property`, calc inside font axes and `color-mix()` were confirmed working there first). Checked in Chrome and Firefox from disk: every section captured at exact progress values on desktop and phone; no element past its column from 360 to 1920 wide; reduced motion and no-JS show finished states with nothing pinned. Two bugs caught on the way: a 220px “Zn” grew a 33px wave crest, so its sliver read as a third of the glyph (crests are now sized per component), and UNLEASH sized in `vw` stretched its grid track until the copy beside it ran to 7px from a 390px phone's edge (now sized in container units). The rules for the old commitments fill, row flood and two-grams vial were removed with them. | `origin.html`, `brand.css` §origin “reading pours”, `site.js` scrub, `styleguide.html` #scrub, `docs/tokens.md` |
| 16 Sep | ~~**The mark is the original red artwork at rest, everywhere, and the tab shows it too.**~~ **Reverted the same day except for the tab icon — see above.** Kash, looking at the live site: the home mark should be ZinStim's original red logo, the way a brand's logo sits in a browser tab. The logo is no longer recoloured, filled or masked. The carbon-fibre artwork shows as it is, clipped to its vector trace, and the vector underneath is filled with the artwork's own average red (#6F2329), so a failed image still leaves a red mark. The tab icon is a 64px square PNG of the artwork, made from the 512px source. The hover liquid moves out of the logo and into the home button: the liquid button's turning blob rises inside the 3rem circle in Zinc Silver. Tried first and dropped: a “clear coat” poured over the logo with blend modes — in Firefox it washed the red out to pink, and a logo should not change colour under a cursor anyway. Checked in Chrome and Firefox from disk with a real pointer: the button fills in about .2s and drains in about .3s while the logo's red pixel count holds (744 at rest, 738 with the button full — edge pixels only). The flat vector (`zinstim-mark.svg`) stays as the one-colour master. | every page, `brand.css` §nav and §the mark, `assets/img/zinstim-favicon.png`, `styleguide.html` #mark, `docs/tokens.md` |
| 16 Sep | **The mark fills with its own carbon fibre, not the elixir.** *(In force again — the red-at-rest change above it was reverted.)* Kash sent the red carbon-fibre artwork and asked for the mark to turn that colour as a liquid reveal on hover. At rest nothing changes — Obsidian on light, Zinc Silver on dark, flat. Under the cursor the same wave-topped liquid rises, but it is now an SVG `<mask>` over the real artwork (`assets/img/zinstim-mark.png`, placed once inside the vector's clip), so the level *uncovers* the picture instead of painting a colour: only the edge moves, the weave never slides, and it is the same artwork on both grounds. The 07 Sep version of this idea — the PNG revealed with CSS `mask-image` — went blank over `file://`; this one has no CSS mask and no cross-origin rule, only an `<image>` and a `<mask>` inside the inline SVG. Checked frame by frame in Chrome, Edge and Firefox, opened from disk, with a real pointer: 0 red pixels at rest; the red rising from the bottom (27% → 37% → 53% of the mark's ink at 53, 129 and 223ms in Chrome); the whole artwork within about half a second; 0 red again about .3s after the pointer leaves. Full is now `--vessel-level: 1.12`, not 1.08: at level 1 the wave's troughs are still 10.5 units below the top of the mark (of 114), and at 1.08 they stopped 1.4 short and drifted along the top of the blade as bumps of the base colour — under a pixel in the nav, plain at 8rem, and there all along under the teal fill too. Caught by diffing two frames that should both be full: 356 pixels changed in the top three rows at 8rem before, 0 after, in Chrome and Firefox. | every page, `brand.css` §the mark, `styleguide.html` #mark |
| 16 Sep | **Every piece on Wear has four views, in a strip under its display.** Front, back, detail, fabric — filling the empty gap between the viewer and the join band that Kash pointed at. Choosing a view swaps the display with a dip: the old drawing sinks into the block while the new one rises out of it. Choosing a view is navigation, so it speaks dots, not liquid — a live teal dot and an Obsidian hairline on the plate. Each piece remembers its own view. The drawings are defined once as symbols and placed with `<use>`, so one edit changes every place a view appears; plates stay uncoloured placeholders with an `<img>` slot. Verified by real clicks: front → back → detail swaps the display and the pressed slot; Floor Short on Fabric while Training Tee stays on Detail. | `wear.html`, `brand.css` §views, `site.js` |
| 16 Sep | ~~**The mark is a vector, Obsidian at rest, and it fills.**~~ **Superseded same day: the mark shows the red artwork at rest — see above. The trace below still holds; it is the artwork's clip and fallback.** Traced from the PNG's alpha by contour detection — IoU **.996**, 25 disagreeing pixels in 6,472, 33 points — and drawn flat: Obsidian on light, Zinc Silver on dark, the mark rule carried in from the start. The red carbon-fibre PNG broke that rule and turned to mud at the 25px it is shown at; it stays in `assets/img` as the master artwork. Pointing at the mark fills it with the elixir — Kash's first liquid reference was a logo filling — which makes the mark the one deliberate crossing of the dots-and-liquid rule: it is the brand, not a control. **What it fills with changed the same day: its own carbon fibre, not the elixir — see above.** Inline in every masthead so it paints over `file://`. The same vector is now the favicon, switching to Zinc Silver in dark browser UI. | every page, `assets/img/zinstim-mark.svg`, `brand.css` §the mark |
| 16 Sep | **Origin's H1 is in colour, and the colour is liquid.** “Confidence / is” hold in Zinc Wash; each phrase in the rolling line pours full with Flame Teal as it lands and drains as it leaves. The type rule against colouring a word is amended rather than broken: colour may enter display type only as the substance filling it, never as a painted accent (`docs/tokens.md`). | `origin.html`, `brand.css` §origin, `docs/tokens.md` |
| 16 Sep | ~~**Origin is in the same system as every other page.**~~ **Reworked same day into reading pours — see above.** The quote and divider fills below still stand. The hollow commitment numerals fill to half as they arrive and to full under the cursor, with their outline kept on top. “What we make” rows fill with a pale body when pointed at and the name takes the elixir; the refusals list is left still on purpose. The zinc section gets a vial holding a poured sliver that never fills — the point is how little two grams is. The quote's “stopped watching.” pours full after its lines rise; the closing “repeated.” pours to half. | `origin.html`, `brand.css` §origin |
| 16 Sep | **The turning blob is only for near-square containers.** On Origin's list rows (~60px tall against a 1,600px square) a straight edge of the rotating square crossed the row as a slanted wedge — captured, and it read as a skewed shape rather than liquid. Rows use the liquid-type construction instead: a pale body with the wave tile on its top edge, rising straight. Recorded in the style guide as “which construction for which shape”. | `brand.css` §origin, `styleguide.html` |
| 16 Sep | **The site is complete enough to hand over.** Every page has a footer that carries the whole site, with the nav's devices (underline drawing in, teal dot on the current page, `aria-current`); a favicon; `theme-color` and share tags. Checked on all seven pages: balanced tags, no duplicate ids, every clip and `<use>` resolves, the same ending, no runtime errors, zero overflow at 390px. | every page |
| 16 Sep | **The style guide is the handoff.** Added a contents index; the mark (live demos at three sizes on both grounds, colour, hover, size, files); liquid constructions by container shape and every place the liquid is used, with live row and numeral demos; views, with a copy-paste snippet for adding a view and the steps for adding a piece (no CSS or JS changes needed); page anatomy — the seven-part skeleton every page follows — and a component inventory mapping 22 components to their classes, CSS section, controller and pages; and how the work is checked. | `styleguide.html` |
| 16 Sep | **One interaction language across every page: dots mean where you are, liquid means what you do.** Kash's direction was to stop designing one page at a time. Two families, never mixed on one control: the nav's dot-to-pill morph stays the language of navigation and selection (the nav, the Wear tabs, the next arrow, status pills); anything a reader acts on fills with the elixir. Four components, one from each of Kash's references, restyled into the brand — flat, one substance, no texture: the **liquid button**, **liquid type**, the **vessel** (liquid SVG) and the **flood** (single-div fill). Documented live in the style guide's new Liquid section. | `brand.css` §liquid, every page, `styleguide.html` |
| 16 Sep | **Every button is a vessel, and hovering fills it** — replacing the 2px hover lift. A turning rounded square rises through the pill and turns only while filled. On light the pill is Obsidian and fills with the elixir; the label never changes because Zinc Wash on the elixir is ~5.4:1. On a dark band it is an empty teal rim that fills with Flame Teal, and the label flips to Obsidian as the surface reaches it. Verified by forcing `:hover` through the DevTools protocol: the liquid travels from below the pill to −22px, label `rgb(233,236,238)` to `rgb(13,13,13)`. | `brand.css` §button |
| 16 Sep | **Page titles are liquid type, resting at a measured half.** Wear, Fuel, Instruments and Elements pour in once on load and fill on hover; the Elements door names fill when their band is chosen (they used to sit at 28% opacity). The word is drawn twice — solid text, and a copy filled with a wave-topped body clipped to the letterforms — so the empty part is full-strength type, never a faint ghost. The rest level is **.5, measured**: at 152px the cap height is .691em inside a 1.32em liquid box, and .5 puts the waterline 51% down the ink. The first value, .6, was a guess and sat 32% down — the words read as teal with black smudges on top. The copy's box is also padded past the word: glyph ink overhangs its advance width, and the unpadded box left a solid sliver on the r of “Wear” even at full — measured at **0 black rows** after the fix. | `brand.css` §liquid, `wear.html`, `fuel.html`, `instruments.html`, `elements.html` |
| 16 Sep | **Product drawings are vessels.** The outline path is used twice — as the clip and as the line on top — so the liquid can never leave the garment and the edge stays crisp over it. The homepage tees fill inside their outline on hover, replacing the inverted second plate (a photograph-swap standing in for photography that does not exist; when real photography lands, the hover becomes the second shot again). Fuel gets a tub that holds a dose at rest and fills when pointed at: Fuel is the one category that *is* the substance, so it is the one page where the vessel is the product. | `index.html`, `fuel.html`, `brand.css` |
| 16 Sep | **Kit cards flood.** The whole card fills with Elixir Deep — flat, because a gradient on a turning shape turns with it. Small muted type fails on Elixir Deep (~2.9:1), so the code and specs brighten to Zinc Silver (~8:1) as the flood reaches them. | `brand.css` §kit |
| 16 Sep | **Every page ends the same way:** dark fracture → the join band → light fracture → marquee → dotmark. There were four different endings — a styled form on the homepage, a bespoke off-system form on Fuel, an inline link on Wear, and nothing at all on Instruments, Elements and Origin — and two broken transitions: Elements dropped a dark fracture straight onto a light marquee, and three pages cut hard from a dark band to light. Checked by script on all six pages: tags balanced, no duplicate ids, every clip reference resolves, and the same ending order everywhere. | every page |
| 16 Sep | **No page carries its own styles any more.** Fuel's and Elements' inline style blocks moved into `brand.css` (their private copies of `.lede`, `.prose`, `.settle` and `.foot` deleted, Fuel's off-system form removed). Underlined inline-styled links became buttons — the homepage's “Read the rest”, Instruments' two doors — so every action on the site is the one liquid component. The Elements subline uses `.lede` like every other page. | `brand.css`, `fuel.html`, `elements.html`, `index.html`, `instruments.html` |
| 16 Sep | **Still at rest; the liquid moves only while it is being filled.** Waves drift and blobs turn only under the cursor or keyboard focus, or once as a title pours in on load, then settle. Touch has no hover, so vessels and door names hold at half and the idea stays visible (checked with touch emulation at 390px, where every page also measured zero horizontal overflow). Reduced motion gets end states with no turning, drift or travel. The style guide's “nothing loops” card was corrected: the 24s surface-drift exception it listed no longer exists. | `brand.css`, `styleguide.html` |
| 16 Sep | **The one moment is a pour.** A vessel tips, the elixir pours, lands, pools and floods the stage, and the claim surfaces as the liquid covers it. From Kash's Figma prototype (*Actual Design*, Desktop 5) and a screen recording of it — taken for its **choreography**, not its surface. The reference is a glossy pink bottle and a psychedelic swirl; this is a flat Obsidian vial with one Zinc Silver band and the elixir family, which is the brand's whole register. It replaces the rectangle that filled between two painted fractures. | `index.html`, `brand.css` §pour, `site.js` |
| 16 Sep | **The top fracture is the liquid's ceiling, not a painted rim — the fix for a cheat Kash caught.** The old rim was an element that turned teal on a ramp from 72% fill, so it went teal before the liquid reached it, and a band of black air showed between a “liquid” rim and the actual liquid. Now there is one liquid shape rebuilt each frame, capped by the fracture line: the teeth only exist where liquid has reached them, so a gap between rim and liquid cannot happen — there is no rim to have one. Verified with an automated check (any stage row more than half Obsidian fails) across every captured frame at 1440×900, 1920×1000, 390×844 and reduced motion: **zero bands**. | `site.js`, `brand.css` §pour |
| 16 Sep | **The claim surfaces exactly as far as the liquid covers it**, measured against the live surface, not a timer — and it sinks back if the reader scrolls up and drains it. Zinc Wash type is invisible on the Zinc Wash page, so the uncovered part of a half-covered line simply is not there: the liquid masks the claim for free, and at 62% through the pour “Two grams” is literally breaking the surface. | `site.js` |
| 16 Sep | **Scroll-driven and pinned, and nothing loops.** The stage pins under the masthead (`--mast-h`, measured, since the masthead is opaque and sticky) and the reader pours it; scrolling back drains it. Even the ripple is a function of progress, so it stops when the reader stops — which retires the looping-surface exception. Tuned against captured frames: the liquid sits **in front** of the vessel so the flood swallows it (behind, the bottle floated on the surface); the vessel leaves before the flood reaches the rim (left in, it poked up between the teeth); the pour starts at 62% of the viewport so the stream lands as the stage pins (at 80% the stage pinned at 43% progress and the pool landed half below the fold). | `site.js`, `brand.css` §pour |
| 16 Sep | **The liquid's gradient follows depth, not the shape's bounding box.** Laid out in stage pixels from the highest surface point, so a thin sheet on the floor stays light and only real depth goes dark; full, the floor is exactly `--elixir-void`, which is what the fracture below is filled with. **Fallbacks:** without JavaScript the section is a plain elixir block with the claim on it, no pin; with reduced motion it is the flooded end state, claim surfaced, no pin and no pour — both captured and checked. | `brand.css` §pour, `site.js` |
| 16 Sep | **Wear: one short wordmark per piece, set large.** `training.` / `floor.` / `short.` at up to 6.4rem, with the full name moved into the code line and kept for screen readers. The reference's “lab.” can be big because it is four characters; “training tee.” at that size would cross half the page. “Small letters” had been over-read as *make the name small* — the reference keeps its details tiny and its wordmark big. The numeral went solid and heavy for the same reason: it is the second anchor on the left, not a faint outline. | `wear.html`, `brand.css` §viewer |
| 16 Sep | **Wear: full width, and sized by height as well as width.** In the 78rem shell a 1920 screen left ~336px of dead margin on the left while the block bled off the right; `.shell--full` uses the frame the way the reference does, and the block starts at column 4 (~26%, reference ~32%). Tall measures use `min(vw, vh)` so the rail and the bottom of the block fit on screen together below the nav — measured at 1920×1000 (682 of 902px), 1440×800 (578 of 702) and 1280×720 (529 of 622). | `wear.html`, `brand.css` §viewer |
| 16 Sep | **Wear: crowding measured by ink, not boxes.** Box overlaps were misleading — grid areas claimed space their text never used. By text ink, the one real collision was the caption, 47px from the garment's collar at 1280; its columns are now sized to their text and it clears by 109–243px at every width. The stamp is pinned to the page edge, not the shell's, after it measured inside the garment's span. | `brand.css` §viewer |
| 16 Sep | **Wear: the action is a real square, and it advances to the next piece.** It measured 33×48 — a sliver — and is now 54×54. It used to jump to the waitlist, sending a reader off the page while they were looking at the product; now it opens the next piece and its label names that piece. The waitlist stays the last thing on the page. **Switching** fires 35 transitions (checked through `getAnimations()`); the leaving block now starts collapsing at .12s instead of .30s, because at .30s the panel had faded before its collapse could be seen. | `wear.html`, `brand.css` §viewer, `site.js` |
| 16 Sep | **Wear on phones: the wordmark had inherited the desktop band row.** The phone grid never reset `grid-template-rows`, so the wordmark's row kept the band's 10rem minimum and sank to its bottom — a 117px hole under the caption, now 30px. The phone action moved to the left end of the block (right-anchored, it sat 15px under the garment) and the garment moved right to clear the wordmark by 47px. | `brand.css` §viewer |
| 16 Sep | **The viewer's layout was rebuilt onto the reference's actual frame.** The first pass took the idea and missed the proportions: it set the piece name at 4.6rem, parked the colour block inside the right-hand column, laid the numeral flat, and ran to 987px — taller than the viewport, so the rail scrolled off and you could never see the control and the thing it controlled at once. The frame now runs across 12 columns the way the reference does: rail and a boxed stamp on its side at the top, then the code and four facts in two tiny columns, then a band holding a numeral turned on its side, the action, the block running off the page edge, and the piece standing on it. Measured at **650px at 1440×900 — it fits the viewport.** | `wear.html`, `brand.css` §viewer |
| 16 Sep | **Small type is the register; exactly one thing is set large, and even that is modest.** Everything on this page except the piece name is .58–.72rem mono. The name is lowercase with a full stop — a wordmark for the piece, not a headline about it — and tops out at 3.2rem against the 4.6rem of the first pass. The composition is carried by the grid and the block. Size was doing work that position should have been doing. | `brand.css` §viewer |
| 16 Sep | **A third scale of the nav's morph, and this one is inside the picture.** The action straddles the block's left edge and opens from a square into a pill with its label rising out of a clip — `.morph` and `.rise` together. Square at rest is the point: it is the only hard-edged mark in a composition of bands, so the eye lands on it. Measured: **33px / 2px radius at rest → 187px / 999px with the label at full opacity and untranslated.** Two of the three nav-derived interactions now sit in the composition rather than above it, which was the other half of why the first pass read as having none. | `brand.css` §viewer |
| 16 Sep | **The plate is sized by height, not width, so the two-tone break cannot drift.** The piece is 150% of the band and hangs from its bottom, so exactly one third clears the top — and that third is what the Obsidian/Zinc-Silver clip line runs off. Giving the plate the artwork's own aspect ratio means the drawing fills it with no letterboxing, so the fraction holds at every viewport: measured **0.333 at both 1440 and 390**. Size it by width instead and the letterbox moves, the clip drifts off the block edge, and the garment changes colour in the wrong place. | `brand.css` §viewer |
| 16 Sep | **The stamp is out of flow.** In flow it was the tallest thing in the header row, so it set that row's height and pushed ~150px of dead air between the rail and the caption — measured. Absolutely positioned it sits at the top-right corner of the frame, where the reference puts it, and the rail alone sets the row. | `brand.css` §viewer |
| 16 Sep | ~~**The fracture above the elixir now tracks the fill instead of being a fixed colour.**~~ **Superseded 16 Sep by the pour — see above.** Kept for the record because it is the mistake the pour fixes: even ramped, the rim was a colour change standing in for liquid, and it went teal before the liquid reached it. Obsidian was right while the vessel was filling — above the waterline is air — and wrong at the brim: a black band on a full vessel reads as a lid, not a rim. `.cleave--lip` takes `--elixir` once the liquid tops out, measured settling at `rgb(11,107,92)`, which is exactly the 0% stop of the body gradient, so there is no colour step. Its opposite number below takes `--elixir-void`, the 100% stop. Top and bottom of one volume. Unlike every reveal on this site the class is **toggled, not added** — a reveal is something you have seen, but a waterline goes back down. | `brand.css` §cleave, `site.js` |
| 16 Sep | ~~**The Wear page is a viewer, not a grid — and it is the nav's gesture at a third scale.**~~ **Layout rebuilt same day — see above.** The interaction survived; the composition did not. §nav turns a dot into a pill; §drop turns a dot into a pill around a status; here the tab's dot opens into a pill AND the stage's block opens out of a circle into the ground the product stands on. Same two properties, same two delays, three sizes of one idea, so a reader who has used the nav already knows the control. Panels stack in a single grid cell and crossfade, so switching never reflows the page under the cursor. The kit grid it replaced was the same list the homepage already shows. | `wear.html`, `brand.css` §viewer, `site.js` |
| 16 Sep | **The garment crosses the edge of the block, so it is drawn twice.** The composition only works if the piece breaks out of the top of the coloured ground — a product sitting neatly inside a rectangle is a catalogue tile, one coming out of it is a photograph. But Zinc Silver on Zinc Wash measures about **1.1:1**, so the part breaking out was invisible. Two clipped copies of the same drawing instead: Obsidian above the line, Zinc Silver below it, both clips driven by `--block-h` so they cannot drift out of register. The piece changes value exactly where it enters the substance. Photography will not need this — it carries its own values. | `brand.css` §viewer |
| 16 Sep | **Wear's dark moment moved to the end of the page.** The viewer wants the light ground the reference gets its space from, so the two cleaves bracketing the old dark kit band went with it. Rather than leave the page with no fracture at all — it is the site's signature transition — the closing "Nothing is for sale yet" band became `band--dark` behind a `cleave--dark`, which is exactly how `index.html` ends. Its body copy moved from Graphite to `--muted`: Graphite on Obsidian measures about 3.5:1. | `wear.html` |
| 16 Sep | **Bleed is computed from the shell's own gutter, not from a percentage.** `calc(50% - 50vw)` is the usual full-bleed trick and it is wrong on a grid item: a percentage margin resolves against the grid **area**, not the container, so it measured the column. The display's right edge landed at **1550 in a 1280 viewport** — 270px past the page, hidden only because the section clips. Replaced with `max(0px,(100vw - 78rem)/2) + gutter`, which is the gap `.shell` actually leaves, and re-measured at 1273. | `brand.css` §viewer |
| 16 Sep | **The runway is gone; the drop replaces it.** The runway was a 340vh sticky track — 2924px of scroll — showing ONE tee, with two of its three spec blocks faded out at any moment. It was also the page's second product display: §kit already says "Everything we have made". Two grids would have been the same list twice. They are now a pair with different jobs — the kit is the catalogue, the drop is the arrival. Document height fell from the runway's bulk to **6118px** measured at 1280×900, and the section's 88-line scroll controller was deleted outright rather than rewritten: the replacement runs on hover and focus alone. | `index.html`, `brand.css` §drop, `site.js` |
| 16 Sep | **The drop's interaction is the nav's, and nothing new was invented for it.** One gesture fires three devices that already existed: (1) **dot to pill** — the state chip uses `.morph`'s exact two-property/two-delay timing, so the obsidian pill closes in around the teal dot and the type inverts to Zinc Wash; (2) **mask-rise** — the spec climbs out of a clip on `.rise`'s timings, same 150% start, same fast-out/slow-in asymmetry; (3) **others recede** — §doors' `:has()` rule, unchanged. Verified on the live page by re-pointing the shipped rules' trigger through CSSOM: the hovered piece holds `opacity:1`, the other settles to `opacity:0.3` / `blur(1.1px)`. | `brand.css` §drop |
| 16 Sep | **Eye direction is spatial, not chromatic — the plates stay uncoloured.** The placeholders are Zinc Silver and will be replaced by photography, so tinting them would spend the colour budget on a rectangle that is going to disappear. What moves the eye instead: the two pieces do not line up (the second drops by up to 5.5rem — the cleave logic applied to layout), the piece you are not looking at recedes, and each piece carries exactly one teal mark. On hover the chosen plate inverts to Obsidian, which is also the only way Flame Teal stays legal on a light page — teal measures 1.7:1 on Zinc Wash, so it needs a dark ground to arrive on. | `brand.css` §drop |
| 16 Sep | **Status is not hidden, only re-grounded.** §kit already settled that a state a reader can act on must be visible at rest. "Waitlist open" is the only actionable thing in this section, so hover changes the ground under the label rather than revealing the label. Consistent with the launch being waitlist-only: there is no price on these cards because nothing is for sale yet. | `index.html`, `brand.css` §drop |
| 16 Sep | **Floor Tee is a real product now, so it was added everywhere it belongs.** The drop shows two tees, which meant a second tee had to exist. "Everything we have made" cannot then omit it, so the kit went from three pieces to four and `wear.html` from two to three. Codes renumbered in page order and kept identical across both pages: ZS—01 Training Tee, ZS—02 Floor Tee, ZS—03 Floor Short, ZS—04 Creatine Mono. | `index.html`, `wear.html` |
| 15 Sep | ~~**The surface is ONE wave, and that is not an aesthetic preference.**~~ **Superseded 16 Sep by the pour — see above.** The lesson still holds for the pour: it builds a single surface shape, never stacked ones. Two overlapping filled wave paths put a hard vertical step in the liquid at the wavelength boundary. Found by rendering each layer alone rather than guessing: back wave clean, front wave carried the seam, bloom innocent, body+rim clean. The front wave's control point sat *above* the baseline (`Q150,14`) where the back wave's sits *below* (`Q150,116`) — that phase is what breaks at the tile edge. If a second surface is ever added, phase it below the baseline like the surviving one, or the step returns. | `brand.css` §elixir, `index.html` |
| 15 Sep | ⚠️ **PROPOSED, not decided — the elixir rows below are awaiting Kash's sign-off.** R2 says nothing is DECIDED until he says so, and an earlier pass wrote these straight into this table and built off them in the same turn. That was wrong. The code exists as a working prototype on one section (the divider) and is reversible. | — |
| 15 Sep | **The elixir: zinc has two states and the brand now shows both.** The zinc tokens are the METAL — inert, cool, what it is on a shelf. The elixir family (`--elixir-void` → `--elixir-glow`) is the same element ACTIVATED. This does not replace TRACE, it completes it: the theme always argued that two grams of a trace element runs everything, and this is the first time you see it. Reference points were Dark Elixir / Chemical X / the Omnitrix — all the same idea, a rare substance that transforms you. | `brand.css` tokens, `styleguide.html` |
| 15 Sep | **Teal — not red, and not violet either.** The first pass picked violet by copying the storyboard's purple, which is taking a reference literally instead of reading it. Read properly, the mood board is near-black and grey almost everywhere with **one** electric colour event: the teal-green car against night asphalt. Red is on the board once, on ASRV — the category default this log already warns about — so it is deliberately not taken. Teal also avoids introducing a rival accent: Flame Teal was already the single live-state colour, and the elixir is depth behind it rather than competition. | `brand.css` tokens |
| 15 Sep | **The elixir is a dark-surface family, more strictly than teal alone.** Elixir Lit on Zinc Wash is **2.2:1** — that fails at every size, large type included — so the elixir never carries copy on a light surface, no exceptions. Flame Teal keeps its one meaning ("live") and reads on the elixir at **9.3:1**. One further measured call: `--elixir-void` sits at **1.2:1** against Obsidian on purpose — at the first value (`#0A0410`) it measured **1.00**, identical, and the liquid disappeared into the band. All figures measured, not eyeballed. | `brand.css`, `styleguide.html` |
| 15 Sep | **Two scoped exceptions, stated rather than sneaked.** *(16 Sep: exception (2) is retired — the pour does not loop at all; its ripple is a function of scroll. Exception (1), gradients inside the liquid, stands.)* (1) *Gradients* — the "no gradients" rule is about **surfaces** and still stands everywhere else; a liquid is a volume with light in it, and painted flat it is just a flat rectangle, so the gradient here is the physics the rule was protecting. (2) *Looping* — the surface drifts continuously (24s) because a liquid that holds still is a solid. The **fill** itself, which is the part that carries meaning, is scroll-driven and never autoplays. Both are scoped to `.elixir` only. | `brand.css` §elixir |
| 15 Sep | **The divider is where it ships first.** *(16 Sep: same moment, same claim — now delivered as a pour. See above.)* "Two grams of it. You never noticed." was always the one moment the brand explains itself — now you watch the two grams arrive and the claim surfaces out of them. Proving one location beats repainting seven pages. `data-reveal` stays on the claim so it is never JS-dependent; the elixir only adds the wet glow. | `index.html`, `site.js` |
| 15 Sep | **The fracture below the elixir is filled with the liquid, not with Obsidian.** *(16 Sep: still true below. The part about the fracture ABOVE staying Obsidian is superseded — there is no painted fracture above any more.)* The fluid is anchored to the bottom of that section, so whatever the fill level is, the thing touching the lower cleave is always liquid — never air. Filled with Obsidian it cut the liquid off at a hard rectangular edge and resumed black, which is why the section read as a teal panel dropped into a black one rather than as a substance that goes somewhere. The new `.cleave--elixir` uses `--elixir-void`, which is not a near-match but the **exact** stop the body gradient ends on at 100% — measured across three columns of a real-page capture, the boundary has no colour step in it at all. The cleave **above** stays Obsidian deliberately: above the waterline is air, and air here is black. Tealing it would show liquid in two disconnected places while the vessel below was still empty. | `brand.css` §cleave, `index.html` |
| 15 Sep | **The mark's crossfade-on-hover (07 Sep entry below) is reverted — it made the mark invisible at rest on every page.** It masked the artwork with `currentColor` for the rest state and only faded in the real PNG on hover. CSS `mask-image` referencing an external file silently fails to paint over the `file://` protocol in Chromium/Edge — proven with matched headless-Edge screenshots of the identical HTML: blank over `file://`, correct over `http://`. This site is routinely opened by double-clicking the HTML, not through a server, so the mark was gone for exactly the readers who never hover. The mark now always shows the real artwork; hover keeps a small translate/rotate instead of a reveal. The Obsidian/Zinc-Silver palette-flip is gone with it — a vanishing nav element outranks that. | `brand.css` §nav |
| 07 Sep | ~~The mark is the real artwork, used twice, masked with `currentColor` at rest and revealed on hover.~~ **Reverted 15 Sep — see above.** | `brand.css` §nav, all pages |
| 07 Sep | **Source art is 244KB–1.9MB and glossy red, so it cannot be dropped in raw.** `ZinstimLogo.svg` is a 1.9MB PNG in an SVG wrapper. `z-emblem.svg` is real vector but a VTracer auto-trace of the render: 2,591 paths, **1,603 fill colours**, 302KB, no `viewBox`, coordinates to x −551 against a declared width of 960 — unusable at 25px and impossible to recolour. What ships is `assets/img/zinstim-mark.png`, the mark resampled to 128px, **23KB**. If a true outline export ever appears (1–2 paths, flat, one colour, proper viewBox), swap it in — the CSS will not need to change. | `assets/img/`, `brand.css` §nav |
| 07 Sep | **Rollcall tracking cannot go tighter than -.012em.** At 900 weight / 78 width the pair "rn" closes into an "m" at display size — "worn" was reading as "worm". Tracking is the fix; it also widens every line, which is why the size clamp came down with it. | `brand.css` §rollcall |
| 07 Sep | **The rollcall is sized by its longest phrase, not by "Confidence".** The slot masks with `overflow:hidden`, which clips sideways as well as down, so an over-long line is cut off mid-word rather than overflowing visibly. Mask height and the distance a word travels are the same custom property (`--slot-h`): a percentage throw resolves against the word's own line box, not the mask, which is how parked words end up peeking along the bottom edge. The hero uses `.shell--full` so display type is not held to the 66ch body measure. Measured clear at 1440 / 1024 / narrow. | `brand.css` §rollcall |
| 07 Sep | **`dotmark.js` sized its canvas from `parent.clientWidth`, which includes padding.** The canvas came out wider than the column it sits in, putting a horizontal scrollbar on every page at any viewport narrower than the shell's max-width. It now subtracts the padding, with `max-width:100%` in CSS as a second line of defence. The four pages that carried their own inline copy of the script were pointed at the shared file, so the fix reaches all of them and ~640 duplicated lines went with it. | `dotmark.js`, `brand.css`, all pages |
| 07 Sep | **Origin opens with the rollcall, and it cycles.** Full-bleed obsidian hero: "Confidence / is / …" where the third line turns through five payoffs. The About-Nike construction, built from our own type. It **opens on the motto** (built, not worn.\*) so the first line read is the one the brand stands on, holds each line 3.2s, and comes back round — roughly a 16s turn. Kash's call, overriding the "nothing loops" line in `tokens.md`; the pace is a reading pace, not a ticker, and it pauses off screen. The footnote is tied to the starred line and comes and goes with it. The four non-motto phrases are `aria-hidden`, so the H1 reads "Confidence is built, not worn." | `brand.css` §rollcall, `site.js`, `origin.html` |
| 07 Sep | **Pull-quote block — the trevornoah.com device.** Marks, one line at scale, hairline, stacked attribution. Reusable on any band, light or dark. **It does not colour a word:** that reference tints one word of the quote, and `tokens.md` forbids accenting a single word in a headline with colour, so emphasis is italic — already the house move. Attribution is self-referential (ZinStim / The Origin) and must stay that way; there are no third-party quotes to publish and inventing one would be the same failure as inventing lab figures. | `brand.css` §quote, `origin.html` |
| 07 Sep | **Motto: Confidence is built, not worn.** The brand line. "Not bought at checkout" is the footnote that explains it. | `origin.html` |
| 07 Sep | **The Dhaka section is cut from Origin.** "Why Dhaka" framed the brand around a location rather than around the argument. The three lab commitments it contained were kept and remounted under "When Fuel goes on sale", which is what they were actually about. Dhaka still appears in the global footer. | `origin.html` |
| 07 Sep | **Wear has its own page. `elements.html` is an index, not a category.** Every "Wear" link used to land on `elements.html`, so clicking Wear showed a page titled Elements and a nav reading Elements — the category and the chooser were the same file. `wear.html` now holds the apparel; `elements.html` stays as the three-door index. The categories are: **Wear = clothing · Fuel = supplements, the main line · Instruments = accessories.** | `wear.html`, `elements.html` |
| 07 Sep | **`instruments.html` says it is empty instead of faking a range.** It was a copy of `elements.html`, so the Instruments door led to the same chooser again. It is now a real page that states there is no supplier, no sample and no date, and links to the two doors that are open. Do not populate it with renders. | `instruments.html` |
| 07 Sep | **Instruments is back as a third door.** Kash's call, and it supersedes the 06 Sep cut below. `instruments.html` exists and is in every nav. The objection in that row still stands — there is no product behind the door yet — so treat the page as a placeholder, not a shipped category. | every page, `brand.css` |
| 07 Sep | **The face swap is a crossfade of two layers, not a font-family switch.** `font-family` cannot be transitioned, so each door carries its word twice — base in Archivo, alternate in the category's face — and hover crossfades them with a spring on the scale. Same construction the Aardvark reference uses. Reverting this to a single element with `:hover{font-family:…}` puts the hard cut back. | `brand.css` §doors |
| 07 Sep | **Door alternates use no new families.** Wear → Instrument Serif italic, Fuel → Space Mono, Instruments → Archivo pushed wide and light (`wdth 125 / wght 300`). The width axis already carries hierarchy everywhere else, so Instruments did not need a fourth font download. | `brand.css` §doors |
| 07 Sep | **Door cards are placeholders and their positions live in CSS.** Four per category, staggered in, positioned by `nth-child` in `brand.css` so no page carries a number. Drop an `<img>` inside a `.door-card` when photography exists. | `brand.css` §doors |
| 07 Sep | **"Wear", not "Worn", everywhere.** Five pages said Worn, `index.html` said Wear, `theme.md` decided Wear. Standardised on the logged name. | every page |
| 06 Sep | **Instruments cut from launch. Two categories: Wear and Fuel.** Nothing exists behind a third door — no product, no supplier, no sample. An empty category is the first promise the site cannot keep, on a brand whose argument is that it does not do that. Add it back when there is something to put in it. | `theme.md`, `elements.html` |
| 06 Sep | **The lab panel ships empty.** Results read "Pending" until an accredited independent lab returns a real batch. Fabricated numbers were removed. The supplier's own COA is also unusable — invalid Salmonella spec, "Complies" in place of figures, blank QC signature, limits 10x looser than their own TDS. | `fuel.html` |
| 06 Sep | **Runway product section.** Sticky centre track, garment travelling as you scroll, specs lighting one at a time, counter at the base. Scroll-driven, never autoplaying. | `brand.css` |
| 06 Sep | **Dotted wordmark closes every page.** Halftone ZINSTIM via `background-clip:text` — real text, not an image. | `brand.css` |
| 06 Sep | **Type: Archivo, not Instrument Serif.** Serif was elegant; the brand needs energy. Heavy+narrow display (900/78), body at 400/95. Width carries hierarchy, not a second typeface. | `brand.css`, `tokens.md` |
| 05 Sep | **Theme: TRACE.** The thing that matters most is the thing you never notice. | `theme.md` |
| 05 Sep | **Zinc is named once**, as a single divider, then never again. No atomic numbers, no lab language. | `theme.md` |
| 05 Sep | **Launch is story-first.** Waitlist only, nothing for sale. Removes the DGDA dependency entirely. | `theme.md` |
| 05 Sep | **Bangladesh first, diaspora second.** Bangla sits beside English with equal standing, not as a toggle. | `theme.md` |
| 05 Sep | Navs: **Origin** · **Elements** → **Wear** / **Fuel** | `theme.md` |
| 05 Sep | **No React.** Static multi-page plus cross-document view transitions. See note below. | `brand.css` |
| 05 Sep | **Section breaks cleave, they do not tear.** Zinc is brittle at room temperature and fractures along crystal planes, so the edge is angular and faceted rather than soft and fibrous. Trevor Noah's effect, derived from our own material. | `brand.css` |
| 05 Sep | **The teal dot means one thing: you are here.** Origin, or the collapsed capsule label. Never two at once. | `brand.css` |

**Rejected, so they don't come back around:** *The Work* (strong but Nike owns
that ground, and it isn't specific to ZinStim). *Signal* (positions you against
Whoop and Oura with a creatine tub — revisit only if accessories ever lead).

### Carried in from earlier brand work
- Logo mark: swept blade-form Z, single closed path, legible at 16px
- Zinc Silver `#D6DDE2` · Obsidian `#0D0D0D` · Flame Teal `#00E0B8`
- Mark never appears in Flame Teal. On light surfaces it flips to Obsidian.
- **Bone `#EFEBE4` dropped** — warm, argued with the material story, and sat too
  close to the default generated-page cream. Replaced by Zinc Wash `#E9ECEE`.
  Reasoning in `tokens.md`.

---

## 4. CURRENT STATE

**Done**
- Figma reviewed. Nav prototype and critique stickies captured in `structure.md`.
- Theme decided: Trace. `theme.md` trimmed to the chosen direction only.
- Tokens written — colour, type, space, motion. `tokens.md`.
- Components specced — nav capsule, three doors, divider. `components.md`.
- Site IA and page structure in `structure.md`.

**Waiting on Kash**
1. Approve the type call — one width-variable family per script (Archivo / Anek Bangla)
2. Approve dropping Bone for Zinc Wash
3. Pick the divider line — three candidates in `02-system/tokens.md`, recommendation noted

**Done (06 Sep)**
- Elements index — two door bands, full viewport each
- Fuel category page with the lab panel in its pending state
- Runway product section and dotted wordmark on the homepage
- Local and chat versions merged into this single flat folder

**Next**
- Real photography to replace the plate placeholders and the SVG tee
- Product detail page — the Aardvark reveal belongs there
- Commission the first independent lab panel, then fill `fuel.html`
- 3D last, and only where it earns its weight

**Fixed in the merge, worth knowing about**
- `elements.html` had an unterminated `<style>` block — everything between the
  page CSS and `</head>` was being parsed as CSS. Closed.
- `fuel.html` shipped invented lab results with "Pass" on every row, and the
  marquee was advertising the invented assay figure. Both removed.
- `.cleave--light` had its fill and background swapped, producing a hard seam
  instead of a fracture. Fixed.
- The `Elements` capsule label was a decorative `aria-hidden` `<span>` with no
  `href` on every page — there was no way to click through to `elements.html`
  itself, only to `Wear`/`Fuel` inside it. Now a real link. `fuel.html`'s
  current-page script also rewrote the label text to "Fuel" without updating
  its `href`, so the visible label pointed at the wrong page — fixed alongside it.

---

## 5. STACK

**No React.** An SPA does not make this site faster, it front-loads the cost.
React plus ReactDOM is roughly 40–50KB gzipped before a line of our own code,
and nothing renders until it downloads and executes. A story-first launch site
gets one or two page views per visitor, so we would pay that on the load that
matters most and never collect the benefit. Bangladesh-first means mobile data
and mid-range Android, which makes it worse. And a brand nobody has heard of
needs to be findable, which client rendering makes harder than it needs to be.

**What we use instead.** Static multi-page, with:

```css
@view-transition { navigation: auto; }
```

Cross-document view transitions fire on same-origin navigation when both pages
opt in. The nav carries `view-transition-name: primary-nav`, so it survives the
page load instead of re-mounting and re-animating — which was the real problem
worth solving. Supported in Chrome 126+, Edge 126+, Safari 18.2+ and iOS 18.2+.
Firefox has not shipped it and gets a normal page load; nothing breaks, because
it is purely a progressive enhancement.

Add speculation rules to prefetch on hover and navigation is effectively
instant.

**If we want component authoring** — so the nav is not copy-pasted into six HTML
files — the answer is **Astro**, not React. It ships static HTML with near-zero
JS, has view transitions built in, and a React component can still be dropped in
later if one specific feature genuinely needs client state.

React becomes correct when there is real client state: a cart, an account, a
configurator. We have none of those, and story-first with a waitlist is the most
static site imaginable.

---

## 6. KNOWN TENSIONS

Flagging these because they will cause churn if left unresolved.

**MUTANT and Under Armour do not match the brief.**
MUTANT is loud, dark, chrome, aggressive. Under Armour is athletic-corporate.
Neither is spacious minimalism. The Awwwards references (illoca, Trevor Noah,
Aardvark) are editorial, playful, experimental — a different world entirely.
Pick a lane. The Awwwards lane is the more distinctive one and matches the
stated brief; MUTANT is the category default.

**The KORA file is the generic default.**
`Shop · Drop 01 — KORA.html` uses cream `#EDE7DB`, serif display, clay accent.
That is the exact combination that reads as AI-generated right now. Useful as a
structural reference for markup. Do not carry its look into ZinStim.

**The AI try-on idea is a product, not a feature.**
The Figma stickies describe photo upload, AI generation, ad-subsidised
subscription, and a self-maintaining agent. That is a separate build with its own
cost model. Park it. It does not belong in v1 of a brand site.
